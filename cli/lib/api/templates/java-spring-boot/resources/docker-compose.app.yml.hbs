version: '3'

services:
  app:
    build: .
    command: gradle --stacktrace run
    volumes:
      - ./src:/app/src
    ports:
      - 8000:8000
  {{#if services.length}}
    depends_on:
  {{/if}}
    {{#each services}}
      - {{this.name}}
    {{/each}}

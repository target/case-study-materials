version: '3'

services:
  test:
    build: .
    command: npm test
    environment:
      - CI=true
    volumes:
      - ./src:/app/src
  {{#if services.length}}
    depends_on:
  {{/if}}
    {{#each services}}
      - {{this.name}}
    {{/each}}

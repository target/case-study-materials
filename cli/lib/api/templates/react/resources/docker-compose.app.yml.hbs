version: '3'

services:
  app:
    build: .
    command: npm start
    environment:
      - PORT=8000
    volumes:
      - ./src:/app/src
    ports:
      - 8000:8000
    depends_on:
      - services
